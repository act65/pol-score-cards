# Only needed if you regenerate scores with the Anthropic API backend
# (build_site_data.py --backend anthropic, or evaluate.py --backend anthropic).
# The default claude_cli backend uses your local `claude` CLI login instead,
# and running the SITE/GAME demos needs no key at all.
ANTHROPIC_API_KEY=sk-ant-...
