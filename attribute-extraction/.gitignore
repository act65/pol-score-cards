*.json

# `*.json` above is for scraped payloads and run scratch. These two are
# CONFIG and PROVENANCE and must travel with the repo — quota_budget.json
# silently failed to commit on 2026-09-17 because of the blanket rule, which
# would have left a fresh clone on the built-in $60/5h fallback, BELOW the
# ceiling the run had already been hitting.
!quota_budget.json
!forthrightness_scores_v3_attempts.json
# Audit sidecars. The markdown reports beside them are already tracked; these
# are the same numbers in machine form, and site/gen_data_stats.py reads them
# to build the "How we check the instrument" section on /data. Untracked, a
# fresh clone rebuilds that page with the section silently missing.
!QUOTE_AUDIT_v3.json
!ATTRIBUTE_OVERLAP_v3.json
!GUESS_VS_SEARCH.json

# Tracked build outputs, regenerable by build_v2_dataset.py. Both are v2.0 —
# a different Civility scale and a retired attribute — so nothing here is
# comparable with what the site serves. Untracked 2026-09-26; still in history,
# which this does NOT shrink.
site_data_full/
site_data_v2/
